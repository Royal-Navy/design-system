{
  "name": "nelson-standards-toolkit",
  "private": true,
  "workspaces": [
    "packages/css-framework",
    "packages/docs-site",
    "packages/eslint-config-react",
    "packages/icon-library",
    "packages/react-component-library",
    "packages/fonts"
  ],
  "engines": {
    "node": ">=10.13.0 <13"
  },
  "devDependencies": {
    "eslint-config-prettier": "^6.9.0",
    "eslint-plugin-prettier": "^3.1.2",
    "husky": "^4.2.1",
    "lerna": "^3.20.2",
    "prettier": "^1.19.1"
  },
  "resolutions": {
    "**/**/handlebars": "^4.5.3",
    "**/**/@hapi/hoek": "^8.5.1",
    "**/**/mem": "^4.0.0",
    "**/**/terser-webpack-plugin": "^1.4.2"
  },
  "prettier": {
    "bracketSpacing": true,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "scripts": {
    "build": "lerna run --stream --concurrency 1 build",
    "lint": "lerna run --parallel lint",
    "lint-staged": "lerna run --parallel lint-staged",
    "test": "lerna run --parallel test",
    "storybook:static": "lerna run --parallel storybook:static",
    "lerna:version": "lerna version --force-publish=* --tag-version-prefix=''"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lerna run --parallel lint-staged"
    }
  }
}
