import {
  getAnimation,
  getBreakpoint,
  getColour,
  getShadow,
  getSpacing,
  getTypography,
  getZIndex
} from './getters'

export default {
  animation: getAnimation,
  breakpoint: getBreakpoint,
  color: getColour,
  shadow: getShadow,
  spacing: getSpacing,
  fontSize: getTypography,
  zIndex: getZIndex
}
