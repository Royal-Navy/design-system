import styled from 'styled-components'

export const StyledOptions = styled.ul`
  max-height: 230px;
  overflow-y: auto;
  list-style-type: none;
  padding-left: 0;
  margin-left: 0;

  &:focus {
    outline: none;
  }
`
