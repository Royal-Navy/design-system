import styled from 'styled-components'

export const StyledExpandButton = styled.button`
  padding: unset;
  border: unset;
  background: unset;
  cursor: pointer;
  height: 16px;
  margin-top: 1px;

  &:hover {
    opacity: 0.75;
  }
`
