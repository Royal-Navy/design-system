const END_ADORNMENT_TYPE = {
  DECREASE: 'decrease',
  INCREASE: 'increase',
} as const

const UNIT_POSITION = {
  AFTER: 'after',
  BEFORE: 'before',
} as const

export { END_ADORNMENT_TYPE, UNIT_POSITION }
