import styled from 'styled-components'
import { color, fontSize } from '@royalnavy/design-tokens'

export const StyledShowingText = styled.span`
  color: ${color('neutral', '400')};
  font-size: ${fontSize('base')};
  white-space: nowrap;
`
